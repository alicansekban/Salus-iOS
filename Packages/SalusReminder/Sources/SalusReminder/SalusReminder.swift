/// Namespace placeholder for the `SalusReminder` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:reminder`.
public enum SalusReminderModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusReminder"
}
