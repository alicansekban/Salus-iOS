/// Namespace placeholder for the `SalusNotifications` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:notifications`.
public enum SalusNotificationsModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusNotifications"
}
