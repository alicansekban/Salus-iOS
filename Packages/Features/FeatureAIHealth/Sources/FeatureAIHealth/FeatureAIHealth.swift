/// Namespace placeholder for the `FeatureAIHealth` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:aihealth`.
public enum FeatureAIHealthModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureAIHealth"
}
