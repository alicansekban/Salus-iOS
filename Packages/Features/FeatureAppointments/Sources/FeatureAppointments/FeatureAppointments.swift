/// Namespace placeholder for the `FeatureAppointments` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:appointments`.
public enum FeatureAppointmentsModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureAppointments"
}
