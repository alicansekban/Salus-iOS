/// Namespace placeholder for the `FeatureVitals` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:vitals`.
public enum FeatureVitalsModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureVitals"
}
