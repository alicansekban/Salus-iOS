/// Namespace placeholder for the `FeatureMedications` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:medications`.
public enum FeatureMedicationsModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureMedications"
}
