/// Namespace placeholder for the `FeaturePaywall` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:paywall`.
public enum FeaturePaywallModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeaturePaywall"
}
