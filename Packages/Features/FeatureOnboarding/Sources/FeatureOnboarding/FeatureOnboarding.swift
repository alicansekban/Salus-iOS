/// Namespace placeholder for the `FeatureOnboarding` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:onboarding`.
public enum FeatureOnboardingModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureOnboarding"
}
