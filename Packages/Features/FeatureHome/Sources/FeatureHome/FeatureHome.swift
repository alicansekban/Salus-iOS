/// Namespace placeholder for the `FeatureHome` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:home`.
public enum FeatureHomeModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureHome"
}
