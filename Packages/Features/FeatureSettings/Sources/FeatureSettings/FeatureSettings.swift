/// Namespace placeholder for the `FeatureSettings` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:settings`.
public enum FeatureSettingsModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureSettings"
}
