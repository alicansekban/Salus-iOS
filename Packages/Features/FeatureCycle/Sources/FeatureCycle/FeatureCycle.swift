/// Namespace placeholder for the `FeatureCycle` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:feature:cycle`.
public enum FeatureCycleModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "FeatureCycle"
}
