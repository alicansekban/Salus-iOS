/// Namespace placeholder for the `SalusCommon` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:common`.
public enum SalusCommonModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusCommon"
}
