/// Namespace placeholder for the `SalusSettings` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:datastore`.
public enum SalusSettingsModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusSettings"
}
