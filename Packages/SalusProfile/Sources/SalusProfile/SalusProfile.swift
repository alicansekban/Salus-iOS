/// Namespace placeholder for the `SalusProfile` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:profile`.
public enum SalusProfileModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusProfile"
}
