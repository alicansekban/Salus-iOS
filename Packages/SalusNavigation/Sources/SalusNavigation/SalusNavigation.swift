/// Namespace placeholder for the `SalusNavigation` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:navigation`.
public enum SalusNavigationModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusNavigation"
}
