/// Namespace placeholder for the `SalusDesignSystem` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:designsystem`.
public enum SalusDesignSystemModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusDesignSystem"
}
