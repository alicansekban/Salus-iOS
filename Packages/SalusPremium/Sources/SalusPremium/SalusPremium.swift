/// Namespace placeholder for the `SalusPremium` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `:core:premium`.
public enum SalusPremiumModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusPremium"
}
