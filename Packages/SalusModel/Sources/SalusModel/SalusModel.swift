/// Namespace marker for the `SalusModel` package.
///
/// M0 ships the package graph plus the theme settings vocabulary
/// (`ThemeSettings.swift`); the rest of the domain arrives in later milestones.
/// Mirrors Android module `:core:model`.
public enum SalusModelModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusModel"
}
