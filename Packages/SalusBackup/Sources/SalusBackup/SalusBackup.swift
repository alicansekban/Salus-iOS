/// Namespace placeholder for the `SalusBackup` package.
///
/// M0 ships the package graph only; the real types arrive in later milestones.
/// Mirrors Android module `(new on both platforms)`.
public enum SalusBackupModule {
    /// Stable module identifier, used to prove the module compiles and links.
    public static let name = "SalusBackup"
}
