# iOS-M6 — Cycle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Executor subagents run on **Opus** (user preference). Compact plan: contracts and behaviour, not source code — the named Kotlin files are the spec. Read `CLAUDE.md`, `docs/ios-feature-template.md` (MANDATORY rules, follow to the letter) and the M4/M5 plans' execution records first. Independent tasks may run in parallel implementers (own worktree + side branch `m6-cycle-tN`, rebased onto `m6-cycle` before review). Branch: `m6-cycle` from `main` (`5a9a7c0`).

**Goal:** `FeatureCycle` — the fifth feature package: the month calendar with recorded/predicted/fertile/ovulation overlay, the one-button start/end period flow, the day log (symptoms, flow, mood, note), the pure `CyclePredictor` (the 11-case table the parity ledger records as owed), the in-screen period-reminder card (toggle, lead days, time) and a real `CycleReminderHandler` whose single occurrence stays a plain notification — with the eight Android test files (41 cases) ported by name and green, predictions never persisted, and the disclaimer always on screen.

**Architecture:** Twin of Android `:feature:cycle`. Persistence exists (M1: four records + tables + indices); M6 adds `CycleDao` in `SalusDatabase`, the missing `LocalDate` arithmetic in `SalusModel`, then the feature package exactly as the template lays it out: `domain/{model,repository,usecase}`, `data/`, `reminder/`, `ui/{calendar,day}`, `navigation/`, `CycleStrings`, `CycleModule`. First feature to read `SalusSettings` (the reminder trio); first month grid in the tree (feature-private, like Android's private composables). Cycle is **not a tab** (Android M9, S-23): it is pushed from the More placeholder now and from the Home card in M7.

**Tech Stack:** Swift 6 / SwiftUI, GRDB (`SalusDatabase`), `SalusReminder` (M3 engine, notification path only), `SalusSettings`, swift-testing.

**Spec:** `salus-android/docs/ios-v1-plan.md` — §4 (module structure), §6.1 (window), §7 (banned claims), §8 (`CycleScreen`, `CycleDayScreen`), iOS-M6 entry ("Calendar + prediction overlay, symptom logging, `CyclePredictor` (port the 11-case table). Predictions are never persisted; disclaimer is mandatory."); `salus-android/docs/parity-ledger.md` S-1, S-4, S-6, S-8, S-12, S-18, S-19, S-23 + roadmap row `Cycle | iOS-M6`. Kotlin sources named per task are the behavioural spec. Research inventories (scratchpad, 845 + 1 734 lines) were transcribed into the "Android reference" blocks below. **Do not modify Android code** — the only Android-repo write is the parity-ledger docs commit in Task 13 (the 2026-08-28 rule: every iOS decision that touches Android enters the ledger in the same milestone).

## Global Constraints

- **Rulings (coordinator, 2026-08-29 — the user was not available; flagged for confirmation, applied unless overruled):**
  1. **Entry point.** No tab. The More placeholder (`App/PlaceholderScreen.swift`) gets a second row, *Regl Takibi* / *Cycle tracking* (`more_cycle` copy is Android's `feature/settings` string; on iOS it lives in the app target's catalog until M8 moves it), pushing `CycleKey()` through `root.navigator`; `cycleDestinations()` + `.environment(\.cycleModule)` go on **both** the More and the Home stacks (Home = placeholder until M7; its card will push the same key). Sex-based row visibility (`MoreViewModel.showCycle`) is M8's, with the hub.
  2. **Reminder tap pushes `CycleKey` onto Home** (`RootTab.hosting(.cyclePeriod) == .home`, then `push(CycleKey())`). Android's tap is a dead end (extras written, never read) — iOS-only behaviour, same class as M4 decision 2; Android follow-up.
  3. **`SalusPillButton` joins `SalusUI`** (`Button` + `.borderedProminent`/`.bordered` in a `Capsule`, `labelLarge`, min height `SalusTouchTarget.min`, optional accent) — Cycle is the third feature to need the M4/M5 inline mapping. The two existing inline sites (appointment detail, medication detail) are **not** migrated in M6 (deferred chore, recorded).
  4. **Reminder dialogs.** Lead days → `confirmationDialog` with the four options (0…3, label = `leadDaysLabel`) + *Vazgeç*; selecting applies immediately (Android's radio dialog does too). Time → `.sheet` carrying a wheel `DatePicker(.hourAndMinute)` seeded from `reminderMinuteOfDay`, *Tamam* emits `hour*60+minute`, *Vazgeç* dismisses. State/events are Android's (`activeReminderDialog`, `reminderDialogRequested/Dismissed`) so `CycleViewModelTests` port 1:1.
  5. **Post-M5 chores land in Task 0**: `CancellationBox`, `latestOfBoth`, `mapped` promoted to `SalusCommon` (three byte-identical copies deleted); `await Task.yield()` drain in the M5 "no write" undo assertions; `salusDismissesKeyboardOnTap()` + `.scrollDismissesKeyboard(.interactively)` on the appointments and vitals editors.
- **Recorded iOS divergences (write into the execution record, not silently):** (a) `saveDayLog` = entry upsert + link replace in **one** write transaction (Android: two DAO calls); (b) `CycleReminderSettings` setters are synchronous (data source is sync); (c) tap → `CycleKey` push (ruling 2); (d) `SalusPillButton` is a SalusUI component (ruling 3); (e) lead-days picker is a confirmation dialog, time picker a sheet (ruling 4); (f) weekday labels come from `Calendar.veryShortStandaloneWeekdaySymbols` reordered Monday-first (Android: `java.time.DayOfWeek` NARROW), the Monday start itself is parity; (g) `CycleDayKey(epochDay:)` keeps Android's payload name — `epochDay` is not an id, so the "`id` never `<noun>Id`" house rule does not apply; (h) the More row and its two strings live in the app target until M8; (i) `observeDailyEntries` and `deletePeriod` are ported without callers (Android dead code, kept for query-for-query parity); (j) `FakeNavigator`/`waitUntil` copied into the feature test target (template-sanctioned); (k) `CyclePredictor` is a `struct` with `callAsFunction(_:today:)`.
- **Never persist a prediction.** `CyclePrediction` has no record, no DAO, no settings key. The disclaimer (`cycle_disclaimer`) is pinned below the scroll area, always visible.
- Local-time semantics (S-18): days are `epochDay`, times `minuteOfDay`; the handler re-derives the trigger instant with `clock.timeZone()` at call time (`clock.instant(of:minuteOfDay:)`). Half-open window `[from, until)`.
- Occurrence identity: `ReminderRef(type: .cyclePeriod, entityId: "cycle-period", occurrenceKey: "\(isoDate)|\(minuteOfDay)")`, e.g. `"2025-09-01|540"`. `presentation` stays `.notification` (S-6), `actions` empty, `onAction` inherited no-op.
- Domain purity: `domain/`, `data/`, `reminder/` import no SwiftUI/UIKit/UserNotifications (`no_ui_framework_in_domain`).
- Strings: **56 Android keys** (`feature/cycle/src/main/res/values{,-en}/strings.xml`), all verbatim, TR source + EN; `%1$s`→`%1$@`, `%1$d`→`%1$lld`; typed `CycleStrings` over `Bundle.module`; TR mixes "Regl" (calendar) and "Dönem" (reminder) — copy as-is. `BannedHealthClaims` repo-wide scan stays green: never write *planlanan*/*planned dose*/*uyum*/*adher* in strings, code, comments or test names — say *tahmin edilen* / *predicted*.
- Colors: `theme.extendedColors.cycle` (period, predicted, button accent) and `theme.extendedColors.vitals` (fertile window — Android reuses the vitals accent, parity). No new tokens; the alphas (0.25, 0.3), border widths (2, 1) and legend dot (12) are Android's file-private constants and stay file-private in Swift.
- Every task: `scripts/test-packages.sh <touched packages>` + `scripts/build-app.sh` green before commit; SwiftFormat/SwiftLint clean (file 500, type 300, function 60, params 6 — split screens into section files); `scripts/clean.sh` after adding a file to a path dependency; `project.yml` + regenerated `pbxproj` in the same commit. Ported test files = Kotlin file name with `Tests.swift`; case names rewritten as prose sentences, case for case; iOS-only cases listed separately.
- Executors report: test names verified against files, every divergence from the Kotlin twin listed, nothing "fixed while passing" outside the task's files.

**Dependency graph:** T0 → {T1, T2, T3, T8} in parallel → T4 (needs T1) → T5 (needs T2+T4) ∥ T6 (needs T4) → T7 (needs T4+T5+T6) → T9 (needs T3+T5+T6) → T10 (needs T8+T9) ∥ T11 (needs T9) → T12 (needs T7, T10, T11) → T13.

---

### Task 0: Branch + post-M5 chores (pure refactor, no behaviour change)

**Files:** Create `Packages/SalusCommon/Sources/SalusCommon/CancellationBox.swift`, `LatestOfBoth.swift` (with `mapped`), `Tests/SalusCommonTests/LatestOfBothTests.swift` (moved). Delete the copies in `FeatureVitals/ui/CancellationBox.swift`, `FeatureAppointments/{ui/CancellationBox,data/LatestOfBoth}.swift`, `FeatureMedications/{ui/CancellationBox,data/LatestOfBoth}.swift` (+ their `LatestOfBothTests`); make the three `public`. Modify the M5 suites' "no write" assertions (`MedicationsViewModelTests`, `MedicationDetailViewModelTests`, `MedicationEditorViewModelTests`) to `await Task.yield()` before reading (precedent `AppointmentsViewModelTests:182`). Modify `AppointmentEditorScreen.swift` and `WeightEditorScreen.swift`: `.salusDismissesKeyboardOnTap()` + `.scrollDismissesKeyboard(.interactively)` as `MedicationEditorScreen` has.

- [ ] `git checkout -b m6-cycle` from `main` (`5a9a7c0`).
- [ ] Promote the three helpers; the three features import them from `SalusCommon` (already a dependency of each). `VitalsRepositoryImpl.swift` keeps its own `mapped` only if the signature differs — otherwise it uses the shared one.
- [ ] Yield drains + keyboard modifiers. `scripts/test-packages.sh` (all) + `scripts/build-app.sh` green. Two commits: `refactor(common): promote CancellationBox, latestOfBoth and mapped to SalusCommon`, `chore: drain undo assertions and dismiss the keyboard on tap in every editor`.

### Task 1: `LocalDate` arithmetic in `SalusModel`

**Files:** Modify `Packages/SalusModel/Sources/SalusModel/LocalDate.swift`; `Tests/SalusModelTests/LocalDateTests.swift`.

**Android reference:** kotlinx-datetime — `LocalDate.plus(n, DAY)`, `plus(n, MONTH)` (day-of-month clamped to the target month's length: Jan 31 + 1 month = Feb 28/29), `daysUntil(other)` (signed, `other − this`), `dayOfWeek.isoDayNumber` (Mon = 1 … Sun = 7); Android's `CycleViewModel` uses `firstDayOfMonth()` = `LocalDate(year, month, 1)`.

**Produces:** `plusDays(_ n: Int) -> LocalDate`, `minusDays(_:)`, `daysUntil(_ other: LocalDate) -> Int`, `plusMonths(_ n: Int) -> LocalDate`, `minusMonths(_:)`, `var firstDayOfMonth: LocalDate`, `var lengthOfMonth: Int`, `var isoDayNumber: Int` (= `mondayBasedWeekdayIndex + 1`).

- [ ] Table tests (write first): ±days across a year boundary and before 1970; `daysUntil` positive/negative/zero; `plusMonths` clamping (2026-01-31 + 1 → 2026-02-28, 2024-01-31 + 1 → 2024-02-29, 2026-03-31 − 1 → 2026-02-28, +12/−12 = same day next/previous year, +0 identity); `lengthOfMonth` for Feb in leap/non-leap, 30/31-day months; `isoDayNumber` for a known Monday and Sunday; `firstDayOfMonth`.
- [ ] Implement over `epochDay`/`init(year:month:day:)` (the normalising init must **not** be used for the month clamp — clamp explicitly). `scripts/test-packages.sh SalusModel` green. Commit `feat(model): LocalDate day and month arithmetic (kotlinx-datetime semantics)`.

### Task 2: `CycleDao` in `SalusDatabase`

**Files:** Create `Packages/SalusDatabase/Sources/SalusDatabase/CycleDao.swift`, `Tests/SalusDatabaseTests/CycleDaoTests.swift`. Template: `MedicationDao.swift` (struct over `SalusDatabase`, Room SQL verbatim in doc comments with `CycleDao.kt:line` citations, streams via `conflatedStream`).

**Android reference:** `core/database/.../dao/CycleDao.kt` — 15 members: `upsertPeriod` (`@Upsert`); `observePeriods(profileId)` = `SELECT * FROM cycle_periods WHERE profile_id = ? ORDER BY start_date DESC`; `getPeriodByStart(profileId, startEpochDay)` = `… AND start_date = ?`; `deletePeriodById(id)`; `upsertDailyEntry`; `getDailyEntry(profileId, epochDay)` = `SELECT * FROM cycle_daily_entries WHERE profile_id = ? AND date = ?`; `observeDailyEntries(profileId, from, until)` = `… date BETWEEN ? AND ? ORDER BY date ASC`; `upsertSymptoms([])`; `observeSymptoms()` = `SELECT * FROM symptoms ORDER BY is_custom ASC, name_key ASC` (global, no profile); `upsertEntrySymptoms([])`; `getEntrySymptoms(entryId)`; `deleteEntrySymptoms(entryId)`; `getOpenPeriod(profileId)` = `… AND end_date IS NULL ORDER BY start_date DESC LIMIT 1`; `countSymptoms()` = `SELECT COUNT(*) FROM symptoms`; `replaceEntrySymptoms(entryId, links)` = `@Transaction { delete; if links non-empty upsert }`. Records: `CyclePeriodRecord(id, profileId, startDateEpochDay, endDateEpochDay?, flowPeak?, note?, createdAtEpochMs: Int64)`, `CycleDailyEntryRecord(id, profileId, dateEpochDay, flow?, mood?, note?)`, `SymptomRecord(id, nameKey, isCustom, iconToken?)`, `CycleEntrySymptomRecord(entryId, symptomId, severity)`. Unique indices `(profile_id, start_date)` and `(profile_id, date)`; `cycle_entry_symptoms` PK `(entry_id, symptom_id)`, FKs cascade.

**Produces (`public struct CycleDao: Sendable`, `init(database:)`):** the 15 members above with the M5 spellings (`observePeriods(profileId:) -> AsyncThrowingStream<[CyclePeriodRecord], any Error>`, `getPeriodByStart(profileId:startEpochDay:)`, `getDailyEntry(profileId:epochDay:)`, `observeDailyEntries(profileId:fromEpochDay:untilEpochDay:)`, `observeSymptoms() -> AsyncThrowingStream<[SymptomRecord], any Error>`, `getOpenPeriod(profileId:)`, `countSymptoms() -> Int`, `replaceEntrySymptoms(entryId:links:)`), **plus iOS-only** `saveDailyEntry(_ entry: CycleDailyEntryRecord, links: [CycleEntrySymptomRecord])` = upsert + replace in **one** `database.writer.write` (divergence (a); the private helper inside the transaction is duplicated, not the public method — M5 rule).

- [ ] Tests (in-memory `SalusDatabase.inMemory(clock:)`): `observePeriods` is DESC and emits on write; `getOpenPeriod` returns the latest open one and nil when all closed; unique `(profile_id, start_date)` rejects a second period on the same start (throws `DatabaseError`); `replaceEntrySymptoms` with empty links leaves none; `saveDailyEntry` is atomic (a link to an unknown symptom id fails the whole write — FK — and no entry row remains); cascade: deleting an entry removes its links; `observeSymptoms` order `is_custom ASC, name_key ASC`; `countSymptoms` 0 → n.
- [ ] `scripts/test-packages.sh SalusDatabase` green. Commit `feat(db): CycleDao ported query-for-query from Room`.

### Task 3: Package setup + strings catalog (`CycleStrings`)

**Files:** Modify `Packages/Features/FeatureCycle/Package.swift` (add `defaultLocalization: "tr"`, `resources: [.process("Resources")]`; keep the eight product deps + `SalusTesting` on the test target only). Delete the M0 stub `FeatureCycle.swift` + its test. Create `Sources/FeatureCycle/Resources/Localizable.xcstrings`, `Sources/FeatureCycle/CycleStrings.swift`, `Tests/FeatureCycleTests/CycleStringsTests.swift`. Modify `project.yml` (register `FeatureCycle` in `packages:` and the app target's `dependencies:`, plus the milestone paragraph in the header comment) + regenerate `Salus.xcodeproj`.

**Android reference:** the 56 keys of `feature/cycle/src/main/res/values{,-en}/strings.xml`, in XML order: `cycle_title`, `cycle_previous_month`, `cycle_next_month`, `cycle_legend_period`, `cycle_legend_predicted`, `cycle_legend_fertile`, `cycle_day_number` (`%1$d`), `cycle_days_until_period` (`%1$d`), `cycle_period_overdue` (`%1$d`), `cycle_no_prediction`, `cycle_confidence` (`%1$s`), `cycle_confidence_low/medium/high`, `cycle_irregular`, `cycle_period_started`, `cycle_period_ended`, `cycle_disclaimer`, `cycle_symptoms_title`, `cycle_flow_title`, `cycle_mood_title`, `cycle_note_label`, `cycle_save`, `cycle_back`, `cycle_symptom_{cramps,headache,mood_swings,bloating,fatigue,tender_breasts,acne,back_pain}`, `cycle_flow_{spotting,light,medium,heavy}`, `cycle_mood_{great,good,neutral,low,irritable,anxious}`, `cycle_reminder_title`, `cycle_reminder_desc`, `cycle_reminder_needs_data`, `cycle_reminder_lead_label`, `cycle_reminder_time_label`, `cycle_reminder_lead_same_day`, `cycle_reminder_lead_days_before` (`%1$d`), `cycle_reminder_cancel`, `cycle_reminder_time_confirm`, `cycle_reminder_notification_title`, `cycle_reminder_notification_body_today`, `cycle_reminder_notification_body_days` (`%1$d`), `cycle_a11y_today`, `cycle_a11y_ovulation`. Copy TR and EN text **from the XML files**, not from memory.

**Produces:** `public enum CycleStrings` with one accessor per key (`static var title`, `static func dayNumber(_ day: Int)`, `daysUntilPeriod(_:)`, `periodOverdue(_:)`, `confidence(_ label: String)`, `reminderLeadDaysBefore(_:)`, `reminderNotificationBodyDays(_:)`, `symptomLabel(nameKey: String) -> String` (the `CycleDayScreen.kt:181-190` switch over the 8 keys, raw key as fallback), `flowLabel(_ level: FlowLevel)`, `moodLabel(_ mood: Mood)`, `confidenceLabel(_ c: CycleConfidence)`); test-visible `Key` enum.

- [ ] `CycleStringsTests`: `StringCatalogParity.assertSourceLanguage("tr")`, `assertKeys(of:are:)` with the literal 56-key set, `assertEveryKeyIsLocalized`, and the value table (TR + EN per key, copied from the XML); `BannedHealthClaims.assertCatalogsNameNothingBanned(paths:)` picks the new catalog up repo-wide.
- [ ] `scripts/test-packages.sh FeatureCycle` + `scripts/build-app.sh` green (the app now links the package). Commit `feat(cycle): package manifest, string catalog and typed CycleStrings (56 keys)`.

### Task 4: Domain — models, repository contracts, `CyclePredictor`, reminder settings contract

**Files:** Create `Sources/FeatureCycle/domain/model/{CyclePeriod,CycleDayLog,Symptom,CyclePrediction}.swift`, `domain/repository/{CycleRepository,CycleReminderSettings}.swift`, `domain/usecase/CyclePredictor.swift`; `Tests/FeatureCycleTests/CyclePredictorTests.swift`, `TestData.swift` (`periods(starts:)` builder: 5-day completed records `period-0…n`, `createdAt = Date(timeIntervalSince1970: 1_750_000_000)`; `startsFromLengths(first:lengths:)`).

**Android reference:** `domain/model/*.kt`, `domain/repository/*.kt`, `domain/usecase/CyclePredictor.kt`, `CyclePredictorTest.kt` (11 cases). Constants: `MIN_RECORDED_STARTS = 2`, `MAX_TRACKED_CYCLES = 6`, `MIN_USABLE_CYCLES = 3`, `MIN/MAX_CYCLE_LENGTH_DAYS = 21/45`, `IRREGULAR_SD_DAYS = 7.0`, `HIGH_CONFIDENCE_SD_DAYS = 3.0`, `LUTEAL_PHASE_DAYS = 14`, fertile window = ovulation −5 … +1. Algorithm: starts `≤ today`, distinct, ascending; `< 2` → nil; `lengths = zipWithNext(daysUntil).takeLast(6)`; `usable = lengths in 21…45`; `considered = usable.ifEmpty { lengths }`; **population** SD of `considered`; `isIrregular = sd > 7 || usable.count < 3`; confidence: irregular → `.low`; `usable.count >= 6 && sd <= 3` → `.high`; else `.medium`; `averageCycleLength = round-half-up(recencyWeightedAverage(considered))` with weights `index + 1` (Kotlin `roundToInt` — use `(x + 0.5).rounded(.down)` semantics, i.e. `Int((x).rounded(.toNearestOrAwayFromZero))` is correct for positive values); `nextPeriodStart = starts.last + average` (never clamped — an overdue prediction stays in the past); `ovulation = next − 14`; `fertileStart = ovulation − 5`, `fertileEnd = ovulation + 1`.

**Produces:** `struct CyclePeriod: Equatable, Hashable, Sendable { id, startDate: LocalDate, endDate: LocalDate?, flowPeak: FlowLevel?, note: String?, createdAt: Date; var isOpen: Bool }`; `struct CycleDayLog { id, date, flow: FlowLevel?, mood: Mood?, note: String?, symptomIds: Set<String> }`; `struct Symptom { id, nameKey, isCustom, iconToken: String? }`; `enum CycleConfidence: String, CaseIterable, Sendable { low = "LOW", medium = "MEDIUM", high = "HIGH" }`; `struct CyclePrediction: Equatable, Sendable { nextPeriodStart, fertileWindowStart, fertileWindowEnd, ovulationDate: LocalDate, averageCycleLength: Int, confidence, isIrregular }`; `protocol CycleRepository: Sendable { observePeriods() -> AsyncThrowingStream<[CyclePeriod], any Error>; getOpenPeriod() async throws -> CyclePeriod?; getPeriodStartingOn(_ date: LocalDate) async throws -> CyclePeriod?; savePeriod(_:) async throws; deletePeriod(id:) async throws; observeSymptoms() -> AsyncThrowingStream<[Symptom], any Error>; getDayLog(on date: LocalDate) async throws -> CycleDayLog?; saveDayLog(_:) async throws }`; `struct CycleReminderConfig: Equatable, Sendable { enabled: Bool, leadDays: Int, minuteOfDay: Int }`; `protocol CycleReminderSettings: Sendable { var config: AsyncStream<CycleReminderConfig> { get }; func setEnabled(_:); func setLeadDays(_:); func setMinuteOfDay(_:) }` (sync setters — divergence (b)); `struct CyclePredictor: Sendable { init(); func callAsFunction(_ periods: [CyclePeriod], today: LocalDate) -> CyclePrediction? }`.

- [ ] Port the 11 cases by name (prose): regular 28-day → HIGH + exact dates; `[24,28,32,24,28,32]` → MEDIUM, avg 29; `[21,45,22,44,23,43]` → LOW irregular; `[28]` → LOW, next = last + 28; `[28,28,60,28,28]` → avg 28, MEDIUM; `[30,30,30,26,26,26]` → avg 27; 8 lengths `[40,40,28,28,28,28,28,28]` → avg 28, HIGH; empty and one period → nil; overdue stays in the past; future-dated start ignored; `[60,60]` → avg 60, LOW.
- [ ] `scripts/test-packages.sh FeatureCycle` green. Commit `feat(cycle): domain models, contracts and CyclePredictor with the 11-case table`.

### Task 5: Data — mappers, `CycleRepositoryImpl` (with seeding), `CycleReminderSettingsImpl`

**Files:** Create `Sources/FeatureCycle/data/{CycleMappers,CycleRepositoryImpl,CycleReminderSettingsImpl}.swift`; `Tests/FeatureCycleTests/{CycleMappersTests,CycleRepositoryImplTests,CycleReminderSettingsImplTests}.swift`.

**Android reference:** `data/CycleMappers.kt` (`DEFAULT_SYMPTOM_SEVERITY = 1`; epochDay↔`LocalDate`, `createdAt` epochMs↔`Date`; enum parse = `FlowLevel(rawValue:)`/`Mood(rawValue:)` → unknown string is **nil, never a throw**; note passed through untrimmed; `toSymptomLinks` one link per id with severity 1). `data/CycleRepositoryImpl.kt` (`profileId = SalusDatabase.defaultProfileId`; `observePeriods` maps the DESC stream; `getDayLog` = entry + links (nil when no entry); `saveDayLog` = `saveDailyEntry(_:links:)` (divergence (a)); `observeSymptoms` = **seed-then-observe**: `if countSymptoms() == 0 { upsertSymptoms(SEED) }` before the DAO stream starts — the `onStart` twin; `SEED_SYMPTOMS` in declaration order `cramps, headache, mood_swings, bloating, fatigue, tender_breasts, acne, back_pain`, id = `"symptom-" + nameKey.replacingOccurrences(of: "_", with: "-")` (`symptom-mood-swings`), `isCustom = false`, `iconToken = nil`). `data/CycleReminderSettingsImpl.kt` (`config` = `dataSource.userSettings` mapped to the trio; setters → `setCycleReminderEnabled/LeadDays/MinuteOfDay`). `CycleMappersTest.kt` (5 cases).

**Produces:** `struct CycleRepositoryImpl: CycleRepository { init(cycleDao: CycleDao, profileId: String) }`; `final class CycleReminderSettingsImpl: CycleReminderSettings { init(dataSource: SalusPreferencesDataSource) }`; `enum CycleMappers` (static functions, `defaultSymptomSeverity = 1`).

- [ ] Mapper tests by name (5): period round-trip (HEAVY); open period + `"NOT_A_LEVEL"` → nils; day log → entry + 2 links with severity 1; entry → domain with symptoms (SPOTTING/GOOD); symptom 1:1. Repository tests (iOS-only, in-memory DB): first `observeSymptoms()` collection seeds exactly 8 rows with the hyphenated ids and emits them in `name_key` order (acne first); a second collection does not duplicate; `saveDayLog` then `getDayLog` round-trips ids; `observePeriods` DESC. Settings tests: `config` reflects the three keys and their defaults (`false`, 1, 540); each setter writes through (fresh `UserDefaults(suiteName:)`).
- [ ] `scripts/test-packages.sh FeatureCycle` green. Commit `feat(cycle): mappers, repository with symptom seeding, reminder settings over SalusSettings`.

### Task 6: Use cases — start, end, save day

**Files:** Create `Sources/FeatureCycle/domain/usecase/{StartPeriodUseCase,EndPeriodUseCase,SaveCycleDayUseCase}.swift`; `Tests/FeatureCycleTests/{StartPeriodUseCaseTests,EndPeriodUseCaseTests,SaveCycleDayUseCaseTests,FakeCycleRepository}.swift`.

**Android reference:** `StartPeriodUseCase(repository, idGenerator)`: `invoke(startDate, createdAt) -> Result { started(CyclePeriod) | alreadyActive | duplicateStart }` — open period exists → `.alreadyActive`; `getPeriodStartingOn(startDate)` exists → `.duplicateStart`; else save `CyclePeriod(id: newId, startDate, endDate: nil, flowPeak: nil, note: nil, createdAt)`. No clock inside, no sync. `EndPeriodUseCase(repository)`: `invoke(endDate) -> { ended(CyclePeriod) | noActivePeriod | invalidEndDate }` — no open → `.noActivePeriod`; `endDate < open.startDate` → `.invalidEndDate`; same-day allowed. `SaveCycleDayUseCase(repository, idGenerator)`: `invoke(date, flow, mood, note, symptomIds) -> Result { saved(CycleDayLog) }` — id preserved from the existing log, note trimmed and blank → nil. `FakeCycleRepository.kt`: in-memory maps; `observePeriods` emits **DESC**; `getOpenPeriod` = open with max start; `observeSymptoms` no seeding; default 3-symptom catalog (`symptom-cramps`, `symptom-headache`, `symptom-fatigue`); `setPeriods`, `setSymptoms`, `currentPeriods()` (ASC), `currentDayLogs()`. Tests: `StartPeriodUseCaseTest` (3), `EndPeriodUseCaseTest` (3), `SaveCycleDayUseCaseTest` (2).

**Produces:** three `struct …UseCase: Sendable` with nested `enum Result: Equatable, Sendable` and `callAsFunction`; `final class FakeCycleRepository: CycleRepository, @unchecked Sendable` (publish outside the lock — the M5 hazard is not to be copied).

- [ ] Port the 8 cases by name; `scripts/test-packages.sh FeatureCycle` green. Commit `feat(cycle): start/end period and save-day use cases`.

### Task 7: `CycleReminderHandler` + notification texts

**Files:** Create `Sources/FeatureCycle/reminder/{CycleReminderHandler,CycleNotificationTexts,LocalizedCycleNotificationTexts}.swift`; `Tests/FeatureCycleTests/{CycleReminderHandlerTests,FakeCycleReminderSettings}.swift`.

**Android reference:** `reminder/CycleReminderHandler.kt` — `type = .cyclePeriod`, `entityId = "cycle-period"` (one pending reminder ever). `currentOccurrence()`: config disabled → nil; `prediction = predictor(periods first value, clock.today())` nil → nil; `.low` → nil; `reminderDate = next − leadDays`; `triggerAt = clock.instant(of: reminderDate, minuteOfDay:)`; key `"\(reminderDate ISO yyyy-MM-dd)|\(minuteOfDay)"`. No past-trigger guard. `occurrencesBetween` = that one if `from <= triggerAt < until`. `notificationContent(ref)`: recompute; nil or key mismatch → nil (stale suppressed); else `ReminderNotificationContent(title: texts.title(), text: texts.body(leadDays:))` — defaults `.notification`, no actions. `AndroidCycleNotificationTexts`: `title` = `cycle_reminder_notification_title`; `body(0)` = `…_body_today`, else `…_body_days(leadDays)`. `CycleReminderHandlerTest.kt` (7): zone Europe/Istanbul, clock `1_755_000_000_000` ms, config `(true, 1, 540)`, stub texts `"title"`/`"body:\(leadDays)"`, seeded starts 2025-05-13/06-10/07-08/08-05 → next 2025-09-02; window `[from, from + 60d)`.

**Produces:** `protocol CycleNotificationTexts: Sendable { func title() -> String; func body(leadDays: Int) -> String }`; `struct LocalizedCycleNotificationTexts: CycleNotificationTexts` (over `CycleStrings`); `public struct CycleReminderHandler: ReminderHandler { init(repository:predictor:settings:clock:texts:) }`; `final class FakeCycleReminderSettings: CycleReminderSettings` (`initial = (false, 1, 540)`, mutable current value + continuation-fed `config` stream).

- [ ] Port the 7 cases by name: one occurrence at 2025-09-01 09:00 Istanbul with key `"2025-09-01|540"`; lead 3 → 08-30; toggle off → none; LOW (two starts) → none; `until = from + 1d` → none; stale key → nil content; matching → `"title"`/`"body:1"`.
- [ ] `scripts/test-packages.sh FeatureCycle` green. Commit `feat(cycle): CycleReminderHandler — one notification, never for LOW confidence`.

### Task 8: `SalusUI` — `SalusPillButton`

**Files:** Create `Packages/SalusUI/Sources/SalusUI/component/SalusPillButton.swift`; test in `Tests/SalusUITests` if the package pins component previews (follow `SalusFilterChip`'s precedent).

**Android reference:** `core/ui/.../SalusPillButton.kt:38-97` — `text`, `onClick`, `enabled = true`, `tonal = false`, `accent: FeatureAccent? = nil`, optional leading/trailing icon; `CircleShape`, `labelLarge`, `heightIn(min = SalusTouchTarget.min)`; filled = accent (or primary) on `onAccent`, tonal = `container`/`onContainer`. iOS mapping settled in M4/M5: `Button` + `.borderedProminent` (filled) / `.bordered` (tonal) + `.tint(accent)` + `.clipShape(Capsule())` (`design-tokens.md` §6 pill row).

**Produces:** `public struct SalusPillButton: View { init(text: String, enabled: Bool = true, tonal: Bool = false, accent: FeatureAccent? = nil, systemImage: String? = nil, action: @escaping () -> Void) }`, `frame(maxWidth: .infinity)` left to the caller.

- [ ] Implement + `#Preview` (`Text(verbatim:)` rule). `scripts/test-packages.sh SalusUI` + build green. Commit `feat(ui): SalusPillButton (core/ui twin) for the third feature that needs it`.

### Task 9: Navigation, `CycleModule`, `CycleViewModel` + `CycleUiState`

**Files:** Create `Sources/FeatureCycle/navigation/CycleNavigation.swift`, `CycleModule.swift`, `ui/calendar/{CycleUiState,CycleViewModel,CycleCalendarBuilder}.swift`; `Tests/FeatureCycleTests/{CycleViewModelTests,FakeNavigator,WaitUntil}.swift`.

**Android reference:** `navigation/CycleNavigation.kt` (`CycleKey`, `CycleDayKey(epochDay)`); `ui/calendar/CycleUiState.kt` (verbatim shapes: `CycleDayCell(epochDay, dayOfMonth, isInMonth, isToday, isPeriod, isPredictedPeriod, isFertile, isOvulation)`, `enum CycleReminderDialog { leadDays, time }`, `CycleUiState(isLoading = true, monthFirstEpochDay = 0, cells = [], hasOpenPeriod = false, cycleDayNumber: Int?, daysUntilNextPeriod: Int?, averageCycleLength: Int?, confidence: CycleConfidence?, isIrregular = false, reminderEnabled = false, reminderLeadDays = 1, reminderMinuteOfDay = 540, reminderHasUsablePrediction = false, activeReminderDialog: CycleReminderDialog?)`, `CycleEvent { previousMonthClicked, nextMonthClicked, startPeriodClicked, endPeriodClicked, reminderToggled(Bool), reminderDialogRequested(CycleReminderDialog), reminderDialogDismissed, reminderLeadDaysSelected(Int), reminderTimeSelected(Int) }`). `ui/calendar/CycleViewModel.kt`: state = combine(monthFirstDay, periods, reminder config, dialog); `monthFirstDay` starts at `clock.today().firstDayOfMonth`; events: month ±1 (`plusMonths`); start → `startPeriod(startDate: today, createdAt: clock.now())` then `reminderScheduler.requestSync()` (**result discarded** — parity); end → `endPeriod(endDate: today)` + sync; toggle → `setEnabled` + sync; dialog requested/dismissed local only; lead days / time → setter + close dialog + sync. `buildState`: `periodDays` = union over periods of `start … (end ?? max(start, today))` (open paints through today); `predictedDays` = predicted range **minus** `periodDays`; predicted length = mean of completed durations (`start.daysUntil(end) + 1`) truncated, clamped 2…10, default 5 when none completed; range `next … next + len − 1`; `fertileDays` = `fertileStart … fertileEnd`; `cycleDayNumber` = `lastStart(≤ today).daysUntil(today) + 1` (no upper clamp); `daysUntilNextPeriod = today.daysUntil(next)` (negative when overdue); `reminderHasUsablePrediction = prediction != nil && confidence != .low`. `buildCells`: **Monday start**: `gridStart = monthStart − (isoDayNumber − 1)`; run to the day before next month, pad to a multiple of 7; every marker is `isInMonth && …` (out-of-month cells carry no flags). `CycleViewModelTest.kt` (6): zone Istanbul, `now = 1_755_000_000_000` ms (today 2025-08-12), id `"new-period"`, sync counter.

**Produces:** `public struct CycleKey: Hashable, Sendable { init() }`, `public struct CycleDayKey: Hashable, Sendable { let epochDay: Int }`, `extension View { public func cycleDestinations() -> some View }` (`CycleKey` → `CycleRoute()`, `CycleDayKey` → `CycleDayRoute(epochDay:)`); `@MainActor public struct CycleModule { repository, navigator, reminderHandler: any ReminderHandler, makeCycleViewModel: () -> CycleViewModel, makeCycleDayViewModel: (Int) -> CycleDayViewModel }`; `@MainActor public func makeCycleModule(cycleDao:preferences: SalusPreferencesDataSource, reminderScheduler:clock:idGenerator:navigator:) -> CycleModule`; `@Entry public var cycleModule: CycleModule?`; `@MainActor @Observable public final class CycleViewModel { init(repository:predictor:startPeriod:endPeriod:clock:reminderSettings:reminderScheduler:); state; onEvent(_:) }` — periods (`AsyncThrowingStream`) and config (`AsyncStream`, wrapped) combine through `SalusCommon.latestOfBoth`; month and dialog are local state that rebuilds synchronously; observation in `init`, cancelled in `deinit` via `CancellationBox`; `enum CycleCalendarBuilder` holds the pure `buildState`/`buildCells` so the grid logic is testable without the VM.

- [ ] Port the 6 cases by name (state after the first loaded emission via `waitUntil`): recorded days + today marked, grid multiple of 7; two periods (Jul 1, Jul 29) → `daysUntilNextPeriod == 14`, `cycleDayNumber == 15`, `averageCycleLength == 28`, `.low`, irregular, Aug 26 predicted, Aug 12 ovulation, Aug 7 fertile; no periods → nils; start then end → open flag and stored `endDate == today`; month navigation Sep 1 / Aug 1; reminder events land in state and `syncRequests == 3`. iOS-only: out-of-month cells carry no flags; open period paints through today; predicted minus recorded.
- [ ] `scripts/test-packages.sh FeatureCycle` green. Commit `feat(cycle): navigation keys, module factory and CycleViewModel with the month grid builder`.

### Task 10: Calendar screen

**Files:** Create `Sources/FeatureCycle/ui/calendar/{CycleScreen,CycleCalendarSections,CycleSummarySections,CycleReminderSections}.swift` (≤ 500 lines each, functions ≤ 60).

**Android reference:** `ui/calendar/CycleScreen.kt` (582 lines). `CycleRoute` = template Route (module from environment, VM in `@State`, `onOpenDay = { navigator.navigate(CycleDayKey(epochDay:)) }`). `CycleScreen(state, onEvent, onOpenDay)`: no scaffold; `SalusScreenHeader(cycle_title)`; loading → centred `ProgressView`; else a scroll view (`padding(.horizontal, lg)`, `spacing: lg`) with **MonthHeader** (chevron.left / title `LLLL yyyy` via `LocalDate.formatted(pattern:locale:)` `titleLarge` / chevron.right; accessibility labels `cycle_previous_month`/`cycle_next_month`), **WeekdayHeader** (7 narrow symbols Monday-first, `labelMedium`, `onSurfaceVariant`), **CalendarGrid** (`cells.chunked(7)` rows, `spacing: xs` both axes, equal-width cells), **CalendarLegend** (three 12-pt dots + `labelSmall`: period `cycle.accent`, predicted `cycle.accent.opacity(0.25)`, fertile `vitals.container`; **no ovulation entry**), **CycleSummaryCard** (`SalusCard`: `cycle_day_number` `titleMedium` if any; `daysUntilNextPeriod` nil → `cycle_no_prediction`, ≥ 0 → `cycle_days_until_period`, < 0 → `cycle_period_overdue(−n)` `bodyMedium`; `cycle_confidence(label)` and `cycle_irregular` `bodySmall` `onSurfaceVariant`; `averageCycleLength` **not rendered**), **CycleReminderCard** (`SalusCard`: title + `Toggle`; subtitle = enabled && !usable ? `cycle_reminder_needs_data` : `cycle_reminder_desc`; when enabled two option rows (label / value in `cycle.accent`, min height `SalusTouchTarget.min`) → `reminderDialogRequested(.leadDays / .time)`; value labels `leadDaysLabel` (0 → `lead_same_day`, else `lead_days_before(n)`) and `HH:mm` 24-hour always), **SalusPillButton** (`hasOpenPeriod ? cycle_period_ended : cycle_period_started`, accent cycle, full width, never disabled). Dialogs per ruling 4. Pinned below the scroll: `cycle_disclaimer` `bodySmall` `onSurfaceVariant` centred, `padding(.horizontal, lg) .padding(.vertical, sm)`. **DayCell**: circle, `aspectRatio(1)`; background `!inMonth` clear / period `cycle.accent` / predicted `cycle.accent.opacity(0.25)` / fertile `vitals.container` / clear; text `!inMonth` `onSurface.opacity(0.3)` / period `cycle.onAccent` / fertile && !predicted `vitals.onContainer` / `onSurface`; border first match: today 2 pt `colorScheme.primary` / predicted 1 pt `cycle.accent` / ovulation 1 pt `vitals.accent`; bold when today; tappable only in month; one merged accessibility label = `[dayOfMonth, a11y_today?, legend_period?, legend_predicted?, legend_fertile?, a11y_ovulation?].joined(", ")`. Typography roles per `design-tokens.md` §9.

- [ ] Build the sections + `#Preview` (loaded, loading, overdue). Manual check in the simulator is Task 13's; here `scripts/build-app.sh` + lint green. Commit `feat(cycle): calendar screen — month grid, legend, summary, reminder card, disclaimer`.

### Task 11: Day screen — `CycleDayViewModel` + `CycleDayScreen`

**Files:** Create `Sources/FeatureCycle/ui/day/{CycleDayUiState,CycleDayViewModel,CycleDayScreen}.swift`; `Tests/FeatureCycleTests/CycleDayViewModelTests.swift`.

**Android reference:** `ui/day/CycleDayUiState.kt` (`CycleSymptomUi(id, nameKey, isSelected)`, `CycleDayUiState(isLoading = true, epochDay, symptoms = [], flow: FlowLevel?, mood: Mood?, noteText = "", isSaving = false)`, `CycleDayEvent { symptomToggled(String), flowSelected(FlowLevel), moodSelected(Mood), noteChanged(String), saveClicked }`); `ui/day/CycleDayViewModel.kt` (`init(epochDay, repository, saveCycleDay, navigator)`: load = first value of `observeSymptoms()` (triggers seeding) + `getDayLog`; preselect; `flowSelected`/`moodSelected` **toggle** (same value again → nil); `saveClicked` → `isSaving = true`, save, `navigator.pop()`; `isSaving` never reset; no delete). `ui/day/CycleDayScreen.kt`: navigation title `d MMMM yyyy`, back = `cycle_back`; sections `cycle_symptoms_title` + `ChipFlowLayout` of `SalusFilterChip` per symptom (`CycleStrings.symptomLabel`), `cycle_flow_title` + chips over `FlowLevel.allCases`, `cycle_mood_title` + chips over `Mood.allCases`, note `TextField` (`cycle_note_label`, sentences capitalisation, ≥ 2 lines, `axis: .vertical`), `SalusPillButton(cycle_save, enabled: !isSaving, accent cycle)`; `.salusDismissesKeyboardOnTap()` + `.scrollDismissesKeyboard(.interactively)`. `CycleDayViewModelTest.kt` (4): date 2026-08-16, id `"new-id"`, `FakeNavigator.commandLog`.

**Produces:** `@MainActor @Observable public final class CycleDayViewModel { init(epochDay:repository:saveCycleDay:navigator:); state; onEvent(_:) }`; `public struct CycleDayRoute: View { init(epochDay: Int) }`; internal `CycleDayScreen(state:onEvent:)`.

- [ ] Port the 4 cases by name: empty day → 3 fake symptoms none selected; existing log preselects (`symptom-cramps` yes, `symptom-headache` no, MEDIUM/LOW/"rough day"); same flow twice clears; save stores `{symptom-fatigue}`, LIGHT, GOOD, "better" and `commandLog == [.pop]`.
- [ ] `scripts/test-packages.sh FeatureCycle` + build green. Commit `feat(cycle): day log screen — symptoms, flow, mood, note`.

### Task 12: App wiring — composition root, shell mount, handler, tap route

**Files:** Modify `App/AppCompositionRoot.swift` (`let cycleModule`, `FeatureModules.cycle`, `makeCycleModule(cycleDao: CycleDao(database:), preferences: infrastructure.preferences, reminderScheduler: reminderRelay, …)` before `makeReminderGraph`; `cycle.reminderHandler` appended to `handlers:`), `App/RootView.swift` (`.more` and `.home` stacks: `.cycleDestinations()` on the root + `.environment(\.cycleModule, root.cycleModule)` on the stack; `openTappedReminder`: `case .cyclePeriod: backStacks.push(CycleKey())` after the tab switch, comment citing ruling 2), `App/PlaceholderScreen.swift` (second row `onOpenCycle`, label from the app catalog `more_cycle` = "Regl Takibi" / "Cycle tracking", subtitle `more_cycle_subtitle` = "Takvim, tahminler ve belirtiler" / "Calendar, predictions and symptoms" — Android `feature/settings` copy, verbatim), `App/RootTab.swift` (doc comment: the push now exists), app catalog + its key-set pin test.

- [ ] Build + full `scripts/test-packages.sh` green; simulator smoke: More → Regl Takibi → calendar renders with today ringed; start period → today fills, button flips; day tap → chips; reminder toggle on with < 2 periods shows `needs_data`. Commit `feat(app): mount Cycle from the More placeholder and the Home stack; register the cycle handler; route its tap`.

### Task 13: Acceptance sweep + execution record + manual QA + parity ledger

**Files:** Modify this plan (execution record: commits per task, review rounds, divergences (a)–(k) + any found, rulings, deferred findings, Android follow-ups); create `scripts/m6-manual-qa.md` (simulator §1 calendar/overlay, §2 start/end + duplicate same-day no-op, §3 day log round-trip + seeding on first open, §4 reminder card → pending notification visible in Reminder Health after sync, §5 device: the notification fires at the configured time and its tap opens the calendar); Android repo docs-only commit on `salus-android/docs/parity-ledger.md`: `D-M6-a…k` rows, proposed follow-ups unnumbered (`A?`): tap dead end (ruling 2), `averageCycleLength` computed but never rendered, use-case results discarded by the VM, `saveDayLog` not transactional, `SalusPillButton` in shared UI, `nav_cycle` unused string; roadmap row `Cycle | iOS-M6 ✓`.

- [ ] `scripts/ci.sh` end to end at the branch tip (5/5 green; paste the summary into the record). Whole-branch final review by the coordinator (Fable). Commit `docs(cycle): M6 execution record and manual QA script`; Android: `docs(parity): iOS-M6 cycle divergences and follow-ups`.

## Self-review notes (written at planning time)

- Spec coverage: calendar + overlay (T9/T10), symptom logging (T11), predictor table (T4), never persisted (global), disclaimer (T10), reminder (T5/T7/T10), 8 test files / 41 cases (T4–T11), entry point (T12), ledger (T13).
- Type consistency checked: `CycleReminderConfig`, `CycleConfidence`, `CycleDayKey(epochDay:)`, `makeCycleModule(cycleDao:preferences:reminderScheduler:clock:idGenerator:navigator:)`, `CyclePredictor.callAsFunction(_:today:)`, `LocalDate.plusMonths/daysUntil/firstDayOfMonth/isoDayNumber` are spelled the same in every task.
- Open for the user: rulings 1–5 above; TR "Regl"/"Dönem" mix copied as-is; whether the More row should already hide for `Sex.male` (deferred to M8 with the hub).
